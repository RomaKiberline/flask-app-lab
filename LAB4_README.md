# Лабораторна робота №4: Об'єкт запиту request. Сесії та кукі

## 📋 Опис завдання

Реалізація системи автентифікації, управління сесіями та cookies у Flask додатку з підтримкою flash повідомлень та вибору кольорової схеми.

## ✅ Виконані завдання

### Завдання 1 (2 бали) - Автентифікація та Flash повідомлення

1. **Flash повідомлення з категоріями**
   - ✅ Підключено підтримку flash повідомлень у базовому шаблоні `base.html`
   - ✅ Реалізовано відображення повідомлень з категоріями: `success`, `error`, `warning`, `info`
   - ✅ Додано стилізацію та анімацію для flash повідомлень
   - ✅ Кнопка закриття повідомлень

2. **Сторінка входу (Login)**
   - ✅ Створено маршрут `/users/login` з формою входу
   - ✅ Поля: username та password
   - ✅ Тестові облікові записи (заглушки):
     - `admin` / `admin123`
     - `user` / `password`
     - `roman` / `flask2024`

3. **Перевірка автентифікації**
   - ✅ POST-запит обробляє дані форми
   - ✅ При правильних даних: збереження в сесію, flash повідомлення, редирект на `/users/profile`
   - ✅ При неправильних даних: flash повідомлення про помилку, повернення на `/users/login`

4. **Сторінка профілю (Profile)**
   - ✅ Відображення привітання з ім'ям користувача з сесії
   - ✅ Перевірка доступу: якщо сесія недоступна → редирект на login з flash повідомленням
   - ✅ Кнопка "Вийти" для виходу з системи
   - ✅ При виході: видалення даних з сесії, flash повідомлення, редирект на login

5. **Навігаційне меню**
   - ✅ Оновлено базовий шаблон з меню:
     - Головна
     - Резюме
     - Профіль
     - Вхід
     - Продукти

### Завдання 2 (2 бали) - Управління Cookies

6. **Інтерфейс управління cookies на сторінці profile**
   - ✅ Форма для додавання cookie:
     - Поле для ключа
     - Поле для значення
     - Поле для терміну дії (в секундах)
   - ✅ Кнопка для видалення всіх cookies
   - ✅ Таблиця з поточними cookies (ключ, значення)
   - ✅ Кнопка видалення окремого cookie
   - ✅ Flash повідомлення про успішне додавання/видалення
   - ✅ Доступ тільки для автентифікованих користувачів

### Завдання 3 (1 бал) - Кольорова схема

7. **Вибір кольорової схеми**
   - ✅ Окремі маршрути для вибору кольору: `/users/set-color-scheme/<scheme>`
   - ✅ Три схеми: `default`, `light`, `dark`
   - ✅ Збереження вибору в cookies (термін дії: 1 рік)
   - ✅ Автоматичне застосування схеми при завантаженні сторінки
   - ✅ Кнопки для зміни кольорової схеми на сторінці profile
   - ✅ Умовні стилі в CSS для різних схем
   - ✅ Динамічне застосування класу до body елемента

## 🗂️ Структура файлів

```
app/
├── __init__.py                          # Ініціалізація Flask додатку
├── users/
│   ├── __init__.py                      # Blueprint для users
│   ├── views.py                         # Маршрути та логіка
│   └── templates/
│       └── users/
│           ├── login.html               # Сторінка входу
│           └── profile.html             # Сторінка профілю
├── templates/
│   └── base.html                        # Базовий шаблон з flash повідомленнями
└── static/
    └── css/
        └── style.css                    # Стилі (flash, форми, теми)
```

## 🔧 Реалізовані маршрути

| Маршрут | Метод | Опис |
|---------|-------|------|
| `/users/login` | GET, POST | Сторінка входу |
| `/users/profile` | GET | Сторінка профілю (потребує автентифікації) |
| `/users/logout` | GET | Вихід з системи |
| `/users/add-cookie` | POST | Додавання cookie |
| `/users/delete-cookie/<key>` | GET | Видалення окремого cookie |
| `/users/delete-all-cookies` | GET | Видалення всіх cookies |
| `/users/set-color-scheme/<scheme>` | GET | Встановлення кольорової схеми |

## 🎨 Функціонал

### Автентифікація
- Декоратор `@login_required` для захисту маршрутів
- Збереження username в сесії Flask
- Автоматичне перенаправлення неавторизованих користувачів

### Flash повідомлення
- 4 категорії: `success`, `error`, `warning`, `info`
- Кольорове кодування
- Анімація появи
- Кнопка закриття

### Управління Cookies
- Додавання з ключем, значенням та терміном дії
- Відображення в таблиці
- Видалення окремих cookies
- Видалення всіх cookies (окрім системних)

### Кольорові схеми
- **Default**: Градієнт фіолетовий (#667eea → #764ba2)
- **Light**: Світлий градієнт (#f5f7fa → #c3cfe2)
- **Dark**: Темний градієнт (#2c3e50 → #34495e)

## 🧪 Тестування

Запустіть тестовий скрипт:

```bash
./venv/bin/python test_lab4.py
```

Тести перевіряють:
1. ✅ Доступність сторінки входу
2. ✅ Невірні дані автентифікації
3. ✅ Правильні дані автентифікації
4. ✅ Захист профілю без входу
5. ✅ Додавання, відображення та видалення cookies
6. ✅ Зміна кольорової схеми
7. ✅ Вихід з системи
8. ✅ Flash повідомлення з категоріями

## 🚀 Запуск додатку

1. Активуйте віртуальне середовище:
```bash
source venv/bin/activate
```

2. Запустіть Flask сервер:
```bash
python run.py
```

3. Відкрийте браузер:
```
http://localhost:5000
```

4. Перейдіть на сторінку входу:
```
http://localhost:5000/users/login
```

## 🔐 Тестові облікові записи

| Username | Password |
|----------|----------|
| admin | admin123 |
| user | password |
| roman | flask2024 |

## 📚 Використані технології

- **Flask**: Веб-фреймворк
- **Flask Sessions**: Управління сесіями
- **Flask Flash**: Flash повідомлення
- **Cookies**: Збереження налаштувань користувача
- **CSS3**: Стилізація та анімації
- **HTML5**: Шаблони

## 🎯 Особливості реалізації

1. **Безпека**:
   - Декоратор `@login_required` для захисту маршрутів
   - Збереження паролів у словнику (для демонстрації)
   - Виключення системних cookies при видаленні

2. **UX/UI**:
   - Сучасний дизайн з градієнтами
   - Плавні анімації та переходи
   - Адаптивні форми
   - Інтуїтивне управління cookies

3. **Функціональність**:
   - Підтримка терміну дії cookies
   - Збереження кольорової схеми на 1 рік
   - Flash повідомлення з кнопкою закриття
   - Динамічна зміна теми без перезавантаження

## 📝 Примітки

- Автентифікація реалізована без бази даних (заглушки)
- SECRET_KEY встановлено в `config.py`
- Системні cookies (session) не видаляються при "Видалити всі"
- Кольорова схема зберігається в cookies з терміном дії 1 рік

## ✨ Додаткові можливості

- Автоматичне закриття flash повідомлень
- Підтвердження при видаленні всіх cookies
- Відображення поточної кольорової схеми
- Таблиця з усіма збереженими cookies
- Стилізовані кнопки для різних кольорових схем

---

**Автор**: Roman Chupyrchuk  
**Дата**: 31.10.2024  
**Гілка**: lab4
